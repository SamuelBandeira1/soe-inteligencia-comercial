<context path="tests" updated="2026-05-30">

pytest suite covering the propensity engine and integration path. Tests run against `app/` (added to `sys.path` in each file). Integration tests skip gracefully when parquets are absent.

## Summary

- `test_propensao_engine.py` — unit tests for `PropensaoEngine`: score components, tier assignment, penalty multipliers; uses synthetic DataFrame fixture
- `test_integracao.py` — integration test: loads real parquet → `PropensaoEngine` → validates output shape and score range; skipped if `data/processed/vendas_filtrada.parquet` missing
- `test_calendar_tw.py` — unit tests for `utils/calendar_tw.py`: TW range calculation, label format, vectorized lookup

## Guidance

- SHOULD add a `conftest.py` if fixture reuse grows (currently each file defines its own fixtures)

</context>
